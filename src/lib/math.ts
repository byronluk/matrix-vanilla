import type { Matrix } from "../types";

/**
 * Multiplies two matrices together
 * @param matrixA - First matrix
 * @param matrixB - Second matrix
 * @returns Resulting matrix
 */
export function multiplyMatrices(matrixA: Matrix, matrixB: Matrix): Matrix {
  // TODO implement matrix multiplication
  return [];
}

/**
 * Validates if two matrices can be multiplied
 * @param matrixA - First matrix
 * @param matrixB - Second matrix
 * @returns Whether matrices can be multiplied
 */
export function canMultiplyMatrices(matrixA: Matrix, matrixB: Matrix): boolean {
  // TODO implement matrix validation
  return false;
}
